{
  "name": "blog_server",
  "version": "1.0.0",
  "description": "Server for Z-Prefix CRUD Application Project (Blog)",
  "main": "app.js",
  "engines": {
    "node": "14.15.1",
    "npm": "6.14.8"
  },
  "scripts": {
    "heroku-postbuild": "cd blog_frontend && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js"
  },
  "author": "Alan Scott",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "pg": "^8.7.1"
  }
}
